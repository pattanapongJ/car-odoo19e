<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Portal users: own bookings only (matched by partner_id).
             Bookings without a partner (still in the public funnel) are
             invisible to portal users — they must complete the funnel first. -->
        <record id="ir_rule_booking_portal_own" model="ir.rule">
            <field name="name">Portal: own bookings</field>
            <field name="model_id" ref="bs_car_booking.model_bs_car_booking"/>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="domain_force">[('partner_id', '=', user.partner_id.id)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Portal users: own documents (via booking's partner). -->
        <record id="ir_rule_booking_document_portal_own" model="ir.rule">
            <field name="name">Portal: own documents</field>
            <field name="model_id" ref="bs_car_booking.model_bs_car_booking_document"/>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="domain_force">[('booking_id.partner_id', '=', user.partner_id.id)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

        <!-- Portal users: own agreements (via booking's partner). -->
        <record id="ir_rule_booking_agreement_portal_own" model="ir.rule">
            <field name="name">Portal: own agreements</field>
            <field name="model_id" ref="bs_car_booking.model_bs_car_booking_agreement"/>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="domain_force">[('booking_id.partner_id', '=', user.partner_id.id)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>
</odoo>
