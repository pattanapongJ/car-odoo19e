<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ================================================================
         REUSABLE HERO MEDIA — uploaded video / direct URL / YouTube /
         Vimeo / image / gradient fallback. Params: car, hero_class,
         fallback_class, img_w.
         ================================================================ -->
    <template id="hero_media" name="Hero Media">
        <t t-set="hero_class" t-value="hero_class or 'car_detail_hero_img'"/>
        <t t-set="img_w" t-value="img_w or '1600x900'"/>
        <t t-if="car.hero_media_type == 'video'">
            <video t-attf-class="{{ hero_class }}" autoplay="autoplay" muted="muted" loop="loop"
                   playsinline="playsinline" preload="metadata" disablepictureinpicture="disablepictureinpicture"
                   t-att-poster="car.image and '/web/image/bs.car.model/%s/image/%s' % (car.id, img_w) or None">
                <source t-att-src="car.hero_video_src" type="video/mp4"/>
            </video>
        </t>
        <t t-elif="car.hero_media_type in ('youtube', 'vimeo')">
            <!-- Poster image is the base layer: it shows behind the video on
                 desktop and REPLACES it on mobile, where YouTube/Vimeo block
                 muted autoplay and force their play/pause/next chrome over the
                 hero (the embed is hidden < lg via .bs_hero_embed). -->
            <img t-if="car.image" t-attf-class="{{ hero_class }} bs_hero_poster"
                 t-att-alt="car.name"
                 t-att-src="'/web/image/bs.car.model/%s/image/%s' % (car.id, img_w)"/>
            <div t-attf-class="bs_hero_embed {{ hero_class }}">
                <iframe t-att-src="car.hero_embed_url" frameborder="0" loading="eager" title="Hero video"
                        tabindex="-1" aria-hidden="true"
                        allow="autoplay; fullscreen"/>
            </div>
        </t>
        <t t-elif="car.image">
            <img t-attf-class="{{ hero_class }}" t-att-alt="car.name"
                 t-att-src="'/web/image/bs.car.model/%s/image/%s' % (car.id, img_w)"/>
        </t>
        <t t-else="">
            <div t-attf-class="{{ hero_class }} {{ fallback_class or '' }}"/>
        </t>
    </template>

    <!-- ================================================================
         BRAND COVER — generic dealer homepage cover.
         Data source: bs.car.website.section(section_type='brand_cover').
         Themes own the visual treatment; this template owns the data contract.
         ================================================================ -->
    <template id="brand_cover_content" name="Brand Cover (content)">
        <t t-set="section" t-value="brand_cover_section or env['bs.car.website.section'].sudo()._get_brand_cover()"/>
        <t t-if="section">
            <div t-attf-class="car_showroom_hero bs_brand_cover #{section.theme_variant or ''}">
                <t t-if="section.video_src">
                    <video t-attf-class="car_showroom_hero_img bs_hero_video #{section.mobile_video_src and 'bs_hero_video_desktop' or ''}"
                           autoplay="autoplay" muted="muted" loop="loop"
                           playsinline="playsinline" preload="metadata"
                           disablepictureinpicture="disablepictureinpicture"
                           t-att-poster="section.image and '/web/image/bs.car.website.section/%s/image/1920x1080' % section.id or None">
                        <source t-att-src="section.video_src" type="video/mp4"/>
                    </video>
                    <video t-if="section.mobile_video_src"
                           class="car_showroom_hero_img bs_hero_video bs_hero_video_mobile"
                           autoplay="autoplay" muted="muted" loop="loop"
                           playsinline="playsinline" preload="metadata"
                           disablepictureinpicture="disablepictureinpicture"
                           t-att-poster="section.image and '/web/image/bs.car.website.section/%s/image/1200x1600' % section.id or None">
                        <source t-att-src="section.mobile_video_src" type="video/mp4"/>
                    </video>
                </t>
                <t t-elif="section.video_embed_url">
                    <div class="bs_hero_embed" aria-hidden="true">
                        <iframe t-att-src="section.video_embed_url"
                                title="Brand video"
                                allow="autoplay; fullscreen"
                                referrerpolicy="strict-origin-when-cross-origin"
                                loading="eager"/>
                    </div>
                </t>
                <t t-if="not section.video_src and section.image">
                    <img t-attf-class="#{section.video_embed_url and 'bs_hero_poster' or 'car_showroom_hero_img'}"
                         t-att-src="'/web/image/bs.car.website.section/%s/image/1920x1080' % section.id"
                         t-att-alt="section.image_alt or section.title"/>
                </t>
                <t t-elif="not section.video_src and section.brand_id and section.brand_id.heritage_image">
                    <img t-attf-class="#{section.video_embed_url and 'bs_hero_poster' or 'car_showroom_hero_img'}"
                         t-att-src="'/web/image/bs.car.brand/%s/heritage_image/1920x1080' % section.brand_id.id"
                         t-att-alt="section.image_alt or section.brand_id.name"/>
                </t>
                <t t-elif="not section.video_src and section.model_id and section.model_id.image">
                    <img t-attf-class="#{section.video_embed_url and 'bs_hero_poster' or 'car_showroom_hero_img'}"
                         t-att-src="'/web/image/bs.car.model/%s/image/1920x1080' % section.model_id.id"
                         t-att-alt="section.image_alt or section.model_id.name"/>
                </t>
                <div t-if="not section.video_src and not section.video_embed_url and not section.image and not (section.brand_id and section.brand_id.heritage_image) and not (section.model_id and section.model_id.image)"
                     class="car_showroom_hero_fallback"/>
                <div class="car_showroom_scrim"/>
                <div class="container car_showroom_content" data-bs-car-reveal="hero">
                    <div class="car_showroom_copy">
                        <span t-if="section.eyebrow or section.brand_id" class="car_kicker">
                            <t t-esc="section.eyebrow or section.brand_id.name"/>
                        </span>
                        <t t-if="section.title_image">
                            <h1 class="visually-hidden" t-esc="section.title"/>
                            <img class="car_hero_title_img"
                                 t-att-src="'/web/image/bs.car.website.section/%s/title_image/1600x440' % section.id"
                                 t-att-alt="section.title_image_alt or section.title"/>
                        </t>
                        <h1 t-else="" class="car_hero_title" t-esc="section.title"/>
                        <p t-if="section.subtitle" class="car_hero_subtitle" t-esc="section.subtitle"/>
                        <div t-if="section.body_html" class="car_hero_subtitle" t-out="section.body_html"/>
                        <div class="car_showroom_actions"
                             t-if="section.primary_cta_url or section.secondary_cta_url">
                            <a t-if="section.primary_cta_url"
                               t-att-href="section.primary_cta_url"
                               class="btn btn-primary car_cta_btn">
                                <t t-esc="section.primary_cta_label or 'Explore'"/>
                                <i class="fa fa-arrow-right"/>
                            </a>
                            <a t-if="section.secondary_cta_url"
                               t-att-href="section.secondary_cta_url"
                               class="btn btn-outline-light car_ghost_btn">
                                <t t-esc="section.secondary_cta_label or 'Learn more'"/>
                            </a>
                        </div>
                    </div>
                </div>
                <div t-if="section.theme_variant in ('fullscreen', 'hongqi_heritage')" class="car_showroom_scroll">
                    <span class="car_showroom_scroll_line"/>
                    <span class="car_showroom_scroll_text">Scroll</span>
                </div>
            </div>
        </t>
        <div t-else="" class="bs_featured_empty alert alert-info text-center">
            Create a published Brand Cover in Website Sections to populate this block.
        </div>
    </template>

    <template id="s_bs_brand_cover" name="Brand Cover">
        <section class="s_bs_brand_cover" data-snippet="s_bs_brand_cover" data-name="Brand Cover">
            <t t-set="brand_cover_section" t-value="False"/>
            <t t-call="bs_car_booking.brand_cover_content"/>
        </section>
    </template>

    <!-- ================================================================
         HOME SECTIONS — canonical data-driven home renderer.
         Data source: bs.car.website.section ordered by sequence.
         ================================================================ -->
    <template id="home_sections_content" name="Home Sections (content)">
        <t t-set="sections" t-value="env['bs.car.website.section'].sudo()._get_home_sections()"/>
        <t t-foreach="sections" t-as="section">
            <t t-set="featured_car" t-value="section._section_model()"/>

            <t t-if="section.section_type == 'brand_cover'">
                <t t-set="brand_cover_section" t-value="section"/>
                <t t-call="bs_car_booking.brand_cover_content"/>
            </t>

            <section t-if="section.section_type == 'featured_model'"
                     class="s_bs_featured_model" data-snippet="s_bs_featured_model" data-name="Featured Car">
                <t t-set="featured_model_section" t-value="section"/>
                <t t-call="bs_car_booking.featured_model_content"/>
            </section>
            <section t-if="section.section_type == 'model_stage'"
                     class="s_bs_model_stage" data-snippet="s_bs_model_stage" data-name="Model Stage">
                <t t-set="model_stage_section" t-value="section"/>
                <t t-call="bs_car_booking.model_stage_content"/>
            </section>
            <section t-if="section.section_type == 'color_studio'"
                     class="s_bs_color_studio car_catalog_band" data-snippet="s_bs_color_studio" data-name="Colour Studio">
                <t t-set="color_studio_section" t-value="section"/>
                <t t-call="bs_car_booking.color_studio_content"/>
            </section>
            <section t-if="section.section_type == 'heritage'"
                     class="s_bs_heritage" data-snippet="s_bs_heritage" data-name="Brand Heritage">
                <t t-call="bs_car_booking.heritage_content"/>
            </section>
            <section t-if="section.section_type == 'model_specs'"
                     class="s_bs_model_specs o_cc o_cc5 o_colored_level"
                     data-snippet="s_bs_model_specs" data-name="Model Specifications"
                     data-oe-shape-data='{"shape":"html_builder/Floats/10","colors":{"c1":"#000000","c2":"#c0392b","c3":"#f1f2f4","c5":"#1d1f25"},"flip":[],"showOnMobile":false,"shapeAnimationSpeed":"0.1"}'>
                <t t-set="model_specs_section" t-value="section"/>
                <t t-call="bs_car_booking.model_specs_content"/>
            </section>
            <section t-if="section.section_type == 'highlights'"
                     class="s_bs_highlights car_catalog_band" data-snippet="s_bs_highlights" data-name="Highlights">
                <t t-set="highlights_section" t-value="section"/>
                <t t-call="bs_car_booking.highlights_content"/>
            </section>
            <section t-if="section.section_type == 'cabin_story'"
                     class="s_bs_cabin_story" data-snippet="s_bs_cabin_story" data-name="Cabin Story">
                <t t-set="cabin_story_section" t-value="section"/>
                <t t-call="bs_car_booking.cabin_story_content"/>
            </section>
            <section t-if="section.section_type == 'latest_arrivals'"
                     class="s_bs_arrivals car_catalog_band" data-snippet="s_bs_arrivals" data-name="Latest Arrivals">
                <t t-call="bs_car_booking.latest_arrivals_content"/>
            </section>
            <section t-if="section.section_type == 'offers'"
                     class="s_bs_offers car_catalog_band" data-snippet="s_bs_offers" data-name="Offers &amp; Promotions">
                <t t-call="bs_car_booking.offers_content"/>
            </section>
            <section t-if="section.section_type == 'stories'"
                     class="s_bs_stories car_catalog_band" data-snippet="s_bs_stories" data-name="Latest News">
                <t t-call="bs_car_booking.stories_content"/>
            </section>
            <section t-if="section.section_type == 'dealer_locator'"
                     class="s_bs_dealers car_catalog_band" data-snippet="s_bs_dealers" data-name="Dealer Locator">
                <t t-call="bs_car_booking.dealer_locator_content"/>
            </section>
            <t t-if="section.section_type == 'hero_slider'">
                <t t-set="hero_slider_section" t-value="section"/>
                <t t-call="bs_car_booking.hero_slider_content"/>
            </t>
        </t>
    </template>

    <!-- ================================================================
         HERO SLIDER — multi-slide hero section.
         Each slide has independent media, copy, and CTA buttons.
         JS: hero_slider.js (auto-advance, prev/next, dots, touch swipe)
         CSS: hero_slider.css
         ================================================================ -->
    <template id="hero_slider_content" name="Hero Slider (content)">
        <t t-set="section" t-value="hero_slider_section or False"/>
        <t t-set="slides" t-value="section and section.slide_ids.filtered(lambda s: s.active) or []"/>
        <t t-if="slides">
            <section class="s_bs_hero_slider car_hero_slider"
                     data-snippet="s_bs_hero_slider" data-name="Hero Slider"
                     t-att-data-interval="(section.slide_interval or 0) * 1000"
                     t-att-data-ken-burns="'1' if section.slide_ken_burns else '0'"
                     role="region"
                     aria-label="Hero Slider"
                     aria-roledescription="carousel"
                     tabindex="0">

                <!-- ARIA live region: screen readers announce slide changes -->
                <div class="bs_slider_live visually-hidden"
                     aria-live="polite" aria-atomic="true"/>

                <!-- ── Slides (display:none by default, JS adds .active) ── -->
                <t t-foreach="slides" t-as="slide">
                    <div t-attf-class="bs_slide #{slide_index == 0 and 'active' or ''}"
                         t-att-data-index="slide_index"
                         role="group"
                         t-att-aria-label="'Slide %s of %s' % (slide_index + 1, len(slides))"
                         aria-roledescription="slide"
                         t-att-aria-hidden="'false' if slide_index == 0 else 'true'">

                        <!-- Background media -->
                        <div class="bs_slide_bg">
                            <t t-if="slide.video_src">
                                <video class="bs_slide_video"
                                       autoplay="autoplay" muted="muted" loop="loop" playsinline="playsinline"
                                       t-att-poster="'/web/image/bs.car.website.slide/%s/image/1920x1080' % slide.id if slide.image else ''">
                                    <source t-att-src="slide.video_src" type="video/mp4"/>
                                </video>
                                <video t-if="slide.mobile_video_src"
                                       class="bs_slide_video_mobile"
                                       autoplay="autoplay" muted="muted" loop="loop" playsinline="playsinline"
                                       t-att-poster="'/web/image/bs.car.website.slide/%s/image/1200x1600' % slide.id if slide.image else ''">
                                    <source t-att-src="slide.mobile_video_src" type="video/mp4"/>
                                </video>
                            </t>
                            <t t-elif="slide.video_embed_url">
                                <!-- Dark fallback shown if iframe fails to load -->
                                <div class="bs_slide_iframe_fallback"/>
                                <iframe class="bs_slide_video"
                                        t-att-src="slide.video_embed_url"
                                        frameborder="0"
                                        allow="autoplay; encrypted-media"
                                        title="Background video"
                                        aria-hidden="true"
                                        style="pointer-events:none;"/>
                            </t>
                            <!-- Expanded srcset: 768w mobile / 1920w desktop / 2560w 4K -->
                            <img t-if="slide.image and not slide.video_src"
                                 class="bs_slide_img"
                                 t-att-src="'/web/image/bs.car.website.slide/%s/image/1920x1080' % slide.id"
                                 t-att-srcset="
                                    '/web/image/bs.car.website.slide/%s/image/768x432' % slide.id   + ' 768w, ' +
                                    '/web/image/bs.car.website.slide/%s/image/1920x1080' % slide.id + ' 1920w, ' +
                                    '/web/image/bs.car.website.slide/%s/image/2560x1440' % slide.id + ' 2560w'
                                 "
                                 sizes="100vw"
                                 t-att-alt="slide.image_alt or slide.title"
                                 loading="lazy"/>
                            <div class="bs_slide_overlay" aria-hidden="true"/>
                        </div>

                        <!-- Content: position controlled by align_x/align_y classes -->
                        <t t-set="_cx" t-value="slide.content_align_x or 'center'"/>
                        <t t-set="_cy" t-value="slide.content_align_y or 'bottom'"/>
                        <t t-set="_tc" t-value="slide.text_color_scheme or 'light'"/>
                        <div t-attf-class="bs_slide_content bs_align_x_{{_cx}} bs_align_y_{{_cy}} bs_text_{{_tc}}">

                            <!-- Text zone -->
                            <div class="bs_slide_text"
                                 t-att-style="
                                     ('color:%s;' % slide.custom_title_color if slide.text_color_scheme == 'custom' and slide.custom_title_color else '')
                                 ">
                                <span t-if="slide.eyebrow" class="bs_slide_eyebrow" t-esc="slide.eyebrow"/>
                                <t t-if="slide.title_image">
                                    <img class="bs_slide_title_img"
                                         t-att-src="'/web/image/bs.car.website.slide/%s/title_image/1200x360' % slide.id"
                                         t-att-alt="slide.title_image_alt or slide.title"
                                         loading="lazy"/>
                                </t>
                                <div t-elif="slide.title" class="bs_slide_title_row">
                                    <h2 class="bs_slide_title" t-esc="slide.title"/>
                                    <span t-if="slide.show_price and slide.price_display"
                                          class="bs_slide_price"
                                          t-esc="slide.price_display"/>
                                </div>
                                <p t-if="slide.subtitle" class="bs_slide_subtitle"
                                   t-att-style="'color:%s;' % slide.custom_subtitle_color if slide.text_color_scheme == 'custom' and slide.custom_subtitle_color else ''"
                                   t-esc="slide.subtitle"/>
                                <div t-if="slide.body_html" class="bs_slide_body" t-out="slide.body_html"/>
                            </div>

                            <!-- CTA buttons (loop — unlimited) -->
                            <div t-if="slide.cta_ids" class="bs_slide_cta">
                                <t t-foreach="slide.cta_ids" t-as="cta">
                                    <t t-set="_cs" t-value="cta.style or 'solid'"/>
                                    <t t-set="_ci" t-value="
                                        ('background:%s;' % cta.bg_color if cta.bg_color else '') +
                                        ('color:%s;' % cta.text_color if cta.text_color else '') +
                                        ('border-color:%s;' % cta.text_color if _cs == 'outline' and cta.text_color else '')
                                    "/>
                                    <!-- Resolve URL by type -->
                                    <t t-set="_car" t-value="slide.section_id.model_id"/>
                                    <t t-if="cta.url_type == 'menu'">
                                        <t t-set="_href" t-value="cta.menu_id.url or '#'"/>
                                    </t>
                                    <t t-elif="cta.url_type == 'book'">
                                        <t t-set="_href" t-value="'/car/%s/book' % _car.id if _car else '#'"/>
                                    </t>
                                    <t t-elif="cta.url_type == 'view'">
                                        <t t-set="_href" t-value="'/car/%s' % _car.id if _car else '#'"/>
                                    </t>
                                    <t t-elif="cta.url_type == 'test_drive'">
                                        <t t-set="_href" t-value="'/test-drive'"/>
                                    </t>
                                    <t t-elif="cta.url_type == 'URL_Download' and cta.download_file">
                                        <t t-set="_href" t-value="'/web/content/bs.car.website.slide.cta/%s/download_file/%s?download=true' % (cta.id, cta.download_filename or 'file')"/>
                                    </t>
                                    <t t-else="">
                                        <t t-set="_href" t-value="cta.url or '#'"/>
                                    </t>
                                    <a t-att-href="_href"
                                       t-attf-class="btn bs_cta_btn bs_cta_style_{{_cs}}"
                                       t-att-style="_ci or None"
                                       t-att-target="'_blank' if cta.target_blank or cta.url_type == 'URL_Download' else None"
                                       t-att-rel="'noopener noreferrer' if cta.target_blank or cta.url_type == 'URL_Download' else None"
                                       t-att-download="cta.download_filename or '' if cta.url_type == 'URL_Download' else None">
                                        <i t-if="cta.icon and cta.icon_position == 'left'"
                                           t-att-class="'fa %s me-2' % cta.icon" aria-hidden="true"/>
                                        <t t-esc="cta.label"/>
                                        <i t-if="cta.icon and (not cta.icon_position or cta.icon_position == 'right')"
                                           t-att-class="'fa %s ms-2' % cta.icon" aria-hidden="true"/>
                                    </a>
                                </t>
                            </div>

                        </div>

                    </div>
                </t>

                <!-- Navigation arrows -->
                <button class="bs_slider_prev" aria-label="Previous slide" aria-controls="hero_slider_track">
                    <i class="fa fa-chevron-left" aria-hidden="true"/>
                </button>
                <button class="bs_slider_next" aria-label="Next slide" aria-controls="hero_slider_track">
                    <i class="fa fa-chevron-right" aria-hidden="true"/>
                </button>

                <!-- Dot indicators -->
                <div class="bs_slider_dots" role="tablist" aria-label="Slide indicators">
                    <t t-foreach="slides" t-as="dot">
                        <button class="bs_slider_dot"
                                role="tab"
                                t-att-data-index="dot_index"
                                t-att-aria-label="'Go to slide %s' % (dot_index + 1)"
                                aria-pressed="false"/>
                    </t>
                </div>

            </section>
        </t>
    </template>

    <!-- ================================================================
         FEATURED MODEL — data-driven content block.
         Self-fetches the featured car so it stays in sync with the catalog;
         reused by the home page template and the drag-drop snippet.
         ================================================================ -->
    <template id="featured_model_content" name="Featured Model (content)">
        <t t-set="section" t-value="featured_model_section or False"/>
        <t t-set="car" t-value="featured_car or (section and section._section_model()) or env['bs.car.model'].sudo()._get_website_featured()"/>
        <t t-if="car">
            <div class="bs_featured o_cc o_cc5">
                <div class="bs_featured_media">
                    <t t-if="section and section.video_src">
                        <video t-attf-class="bs_featured_img bs_hero_video #{section.mobile_video_src and 'bs_hero_video_desktop' or ''}"
                               autoplay="autoplay" muted="muted" loop="loop"
                               playsinline="playsinline" preload="metadata"
                               disablepictureinpicture="disablepictureinpicture"
                               t-att-poster="section.image and '/web/image/bs.car.website.section/%s/image/1600x900' % section.id or None">
                            <source t-att-src="section.video_src" type="video/mp4"/>
                        </video>
                        <video t-if="section.mobile_video_src"
                               class="bs_featured_img bs_hero_video bs_hero_video_mobile"
                               autoplay="autoplay" muted="muted" loop="loop"
                               playsinline="playsinline" preload="metadata"
                               disablepictureinpicture="disablepictureinpicture"
                               t-att-poster="section.image and '/web/image/bs.car.website.section/%s/image/1200x1600' % section.id or None">
                            <source t-att-src="section.mobile_video_src" type="video/mp4"/>
                        </video>
                    </t>
                    <t t-elif="section and section.video_embed_url">
                        <div class="bs_hero_embed" aria-hidden="true">
                            <iframe t-att-src="section.video_embed_url"
                                    title="Featured model video"
                                    allow="autoplay; fullscreen"
                                    referrerpolicy="strict-origin-when-cross-origin"
                                    loading="eager"/>
                        </div>
                        <img t-if="section.image"
                             class="bs_featured_img bs_hero_poster"
                             t-att-src="'/web/image/bs.car.website.section/%s/image/1600x900' % section.id"
                             t-att-alt="section.image_alt or section.title or car.name"/>
                    </t>
                    <t t-elif="section and section.image">
                        <img class="bs_featured_img"
                             t-att-src="'/web/image/bs.car.website.section/%s/image/1600x900' % section.id"
                             t-att-alt="section.image_alt or section.title or car.name"/>
                    </t>
                    <t t-else="">
                        <t t-call="bs_car_booking.hero_media">
                            <t t-set="hero_class" t-value="'bs_featured_img'"/>
                            <t t-set="fallback_class" t-value="'bs_featured_fallback'"/>
                            <t t-set="img_w" t-value="'1600x900'"/>
                        </t>
                    </t>
                </div>
                <div class="bs_featured_scrim"/>
                <div class="container bs_featured_copy">
                    <span class="car_kicker" t-esc="(section and section.eyebrow) or 'Featured Model'"/>
                    <t t-if="section and section.title_image">
                        <h2 class="visually-hidden" t-esc="section.title or car.name"/>
                        <img class="bs_featured_title_img"
                             t-att-src="'/web/image/bs.car.website.section/%s/title_image/1200x360' % section.id"
                             t-att-alt="section.title_image_alt or section.title or car.name"/>
                    </t>
                    <h2 t-else="" class="bs_featured_title">
                        <t t-esc="(section and section.title) or '%s %s' % (car.brand_id.name, car.name)"/>
                    </h2>
                    <p t-if="section and section.subtitle" class="bs_featured_subtitle" t-esc="section.subtitle"/>
                    <div t-if="section and section.body_html" class="bs_featured_subtitle" t-out="section.body_html"/>
                    <t t-set="hl" t-value="car.spec_ids.filtered('is_highlight')[:4]"/>
                    <div t-if="hl" class="bs_featured_specs">
                        <div class="bs_featured_spec" t-foreach="hl" t-as="sp">
                            <strong t-esc="sp.value"/>
                            <span><t t-esc="sp.unit"/> <t t-esc="sp.name"/></span>
                        </div>
                    </div>
                    <div class="bs_featured_actions">
                        <a t-att-href="(section and section.primary_cta_url) or '/car/%s' % car.id"
                           class="btn btn-primary car_cta_btn">
                            <t t-esc="(section and section.primary_cta_label) or 'Discover'"/>
                            <i class="fa fa-arrow-right"/>
                        </a>
                        <a t-if="not section or section.secondary_cta_url or section.secondary_cta_label"
                           t-att-href="(section and section.secondary_cta_url) or '/car/%s/book' % car.id"
                           class="btn btn-outline-light car_ghost_btn">
                            <t t-esc="(section and section.secondary_cta_label) or 'Configure'"/>
                        </a>
                    </div>
                </div>
            </div>
        </t>
        <div t-else="" class="bs_featured_empty alert alert-info text-center">
            Publish a car model (and tick <b>Featured on Home</b>) to populate this section.
        </div>
    </template>

    <!-- Drag-drop snippet wrapper: the t-call keeps the block server-rendered
         (always in sync) wherever it is dropped. -->
    <template id="s_bs_featured_model" name="Featured Car">
        <section class="s_bs_featured_model" data-snippet="s_bs_featured_model" data-name="Featured Car">
            <t t-set="featured_car" t-value="False"/>
            <t t-set="featured_model_section" t-value="False"/>
            <t t-call="bs_car_booking.featured_model_content"/>
        </section>
    </template>

    <!-- ================================================================
         MODEL SPECIFICATIONS — data-driven spec grid (replaces the hardcoded
         "E-HS9 Specifications" block) from the featured model's spec_ids.
         ================================================================ -->
    <template id="model_specs_content" name="Model Specs (content)">
        <t t-set="section" t-value="model_specs_section or False"/>
        <t t-set="car" t-value="featured_car or (section and section._section_model()) or env['bs.car.model'].sudo()._get_website_featured()"/>
        <t t-if="car and car.spec_ids">
            <div class="o_we_shape o_html_builder_Floats_10 bs_specs_shape"
                 style="background-image: url('/html_editor/shape/html_builder%2FFloats%2F10.svg?c1=%23000000&amp;c2=%23c0392b&amp;c3=%23f1f2f4&amp;c5=%231d1f25&amp;shapeAnimationSpeed=0.1');"/>
            <div class="container bs_specs">
                <div class="bs_specs_title">
                    <span class="bs_specs_line"/>
                    <h2>
                        <t t-esc="(section and section.title) or '%s Specifications' % car.name"/>
                    </h2>
                    <span class="bs_specs_line"/>
                </div>
                <p t-if="section and section.subtitle" class="bs_specs_subtitle" t-esc="section.subtitle"/>
                <div class="bs_specs_grid">
                    <div class="bs_spec_cell" t-foreach="car.spec_ids.sorted('sequence')" t-as="sp">
                        <div class="bs_spec_lbl">
                            <t t-esc="sp.name"/>
                        </div>
                        <div t-attf-class="bs_spec_val #{sp.is_highlight and 'is-gold' or ''}"
                             t-esc="sp.display_value"/>
                    </div>
                </div>
            </div>
        </t>
        <div t-else="" class="bs_featured_empty alert alert-info text-center">
            Add specifications to the featured car model (Specifications tab) to populate this section.
        </div>
    </template>
    <template id="s_bs_model_specs" name="Model Specifications">
        <section class="s_bs_model_specs o_cc o_cc5 o_colored_level"
                 data-snippet="s_bs_model_specs" data-name="Model Specifications"
                 data-oe-shape-data='{"shape":"html_builder/Floats/10","colors":{"c1":"#000000","c2":"#c0392b","c3":"#f1f2f4","c5":"#1d1f25"},"flip":[],"showOnMobile":false,"shapeAnimationSpeed":"0.1"}'>
            <t t-set="featured_car" t-value="False"/>
            <t t-set="model_specs_section" t-value="False"/>
            <t t-call="bs_car_booking.model_specs_content"/>
        </section>
    </template>

    <!-- ================================================================
         DEALER LOCATOR — showrooms from bs.car.dealer (uses lat/long for
         "Get directions"). Plus AutoDealer JSON-LD for SEO.
         ================================================================ -->
    <!-- Callers may pre-set `dealers` (e.g. the car detail page passes the
         brand's dealers) and override the heading via locator_kicker/
         locator_title; the home section self-fetches every published dealer. -->
    <template id="dealer_locator_content" name="Dealer Locator (content)">
        <t t-set="current_website" t-value="env['website'].get_current_website()"/>
        <t t-set="dealers" t-value="dealers or env['bs.car.dealer'].sudo().with_context(website_id=current_website.id).search(
            [('active', '=', True), ('website_published', '=', True), ('company_id', 'in', [False, current_website.company_id.id])], order='sequence, id')"/>
        <t t-if="dealers">
            <div class="container bs_dealers">
                <div class="section_header_center">
                    <span class="car_kicker car_kicker_dark" t-esc="locator_kicker or 'Our showrooms'"/>
                    <h2 class="section_title" t-esc="locator_title or 'Find your preferred location'"/>
                </div>
                <div class="bs_dealers_locator">
                    <div class="bs_dealers_mapwrap">
                        <iframe t-if="dealers[0].map_embed_url" class="bs_dealers_map"
                                t-att-src="dealers[0].map_embed_url"
                                title="Showroom location" loading="lazy"
                                referrerpolicy="no-referrer-when-downgrade"
                                allowfullscreen="allowfullscreen"/>
                        <div class="bs_dealers_map_filter" aria-hidden="true"/>
                    </div>
                    <div class="bs_dealers_list" role="list">
                        <div t-foreach="dealers" t-as="d" role="listitem"
                             t-attf-class="bs_dealer_card#{d_first and ' is_active' or ''}"
                             t-att-data-map-src="d.map_embed_url"
                             tabindex="0" t-att-aria-label="d.name">
                            <div class="bs_dealer_card_head">
                                <h4 class="bs_dealer_name" t-esc="d.name"/>
                                <span t-if="d.city" class="bs_dealer_city" t-esc="d.city"/>
                            </div>
                            <div class="bs_dealer_meta">
                                <div t-if="d.street or d.city" class="bs_dealer_line">
                                    <i class="fa fa-map-marker"/>
                                    <span><t t-esc="d.street"/><t t-if="d.street and d.city">, </t><t t-esc="d.city"/></span>
                                </div>
                                <div t-if="d.phone" class="bs_dealer_line">
                                    <i class="fa fa-phone"/>
                                    <a t-attf-href="tel:{{ d.phone }}" t-esc="d.phone"/>
                                </div>
                                <div t-if="d.opening_hours" class="bs_dealer_line bs_dealer_hours">
                                    <i class="fa fa-clock-o"/>
                                    <span t-esc="d.opening_hours"/>
                                </div>
                            </div>
                            <a t-if="d.directions_url" class="bs_dealer_directions"
                               target="_blank" rel="noopener" t-att-href="d.directions_url">
                                <i class="fa fa-location-arrow"/> Directions
                            </a>
                        </div>
                    </div>
                </div>
            </div>
            <script type="application/ld+json"
                    t-out="env['bs.car.dealer'].sudo()._website_autodealer_jsonld()"/>
        </t>
        <div t-else="" class="bs_featured_empty alert alert-info text-center">
            Publish a dealer/showroom to populate this section.
        </div>
    </template>
    <template id="s_bs_dealers" name="Dealer Locator">
        <section class="s_bs_dealers car_catalog_band" data-snippet="s_bs_dealers" data-name="Dealer Locator">
            <t t-call="bs_car_booking.dealer_locator_content"/>
        </section>
    </template>

    <!-- ================================================================
         OFFERS & PROMOTIONS — time-boxed campaign cards from bs.car.offer.
         Only offers within their validity window are shown (date-driven).
         ================================================================ -->
    <template id="offers_content" name="Offers (content)">
        <t t-set="offers" t-value="env['bs.car.offer'].sudo()._get_active_offers(
            model_id=featured_car and featured_car.id or None)"/>
        <t t-if="offers">
            <div class="container bs_offers">
                <div class="section_header_center">
                    <span class="car_kicker car_kicker_dark">Current offers</span>
                    <h2 class="section_title">Deals worth driving home</h2>
                </div>
                <div class="bs_offer_grid">
                    <div class="bs_offer_card" t-foreach="offers" t-as="o">
                        <div class="bs_offer_media">
                            <img t-if="o.image" t-att-alt="o.name" loading="lazy"
                                 t-att-src="'/web/image/bs.car.offer/%s/image/720x480' % o.id"/>
                            <div t-else="" class="bs_offer_media_fallback"/>
                            <span t-if="o.badge" class="bs_offer_badge" t-esc="o.badge"/>
                        </div>
                        <div class="bs_offer_body">
                            <h3 class="bs_offer_title" t-esc="o.name"/>
                            <p t-if="o.subtitle" class="bs_offer_subtitle" t-esc="o.subtitle"/>
                            <div t-if="o.description" class="bs_offer_desc" t-out="o.description"/>
                            <div class="bs_offer_footer">
                                <small t-if="o.date_end" class="bs_offer_until">
                                    Until <t t-esc="o.date_end" t-options="{'widget': 'date'}"/>
                                </small>
                                <t t-set="cta" t-value="o._resolve_cta_url()"/>
                                <a t-if="cta" t-att-href="cta" class="btn btn-primary car_cta_btn btn-sm">
                                    <t t-esc="o.cta_label or 'Learn more'"/> <i class="fa fa-arrow-right"/>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </t>
        <div t-elif="not featured_car" class="bs_featured_empty alert alert-info text-center">
            Publish a promotional offer (and set its validity dates) to populate this section.
        </div>
    </template>
    <template id="s_bs_offers" name="Offers &amp; Promotions">
        <section class="s_bs_offers car_catalog_band" data-snippet="s_bs_offers" data-name="Offers &amp; Promotions">
            <t t-set="featured_car" t-value="False"/>
            <t t-call="bs_car_booking.offers_content"/>
        </section>
    </template>

    <!-- ================================================================
         BRAND HERITAGE — cinematic brand-story band (image + narrative).
         Data-driven from the featured bs.car.brand (heritage_* fields).
         ================================================================ -->
    <template id="heritage_content" name="Brand Heritage (content)">
        <t t-set="brand" t-value="env['bs.car.brand'].sudo()._get_website_featured()"/>
        <t t-if="brand">
            <div class="bs_heritage">
                <div t-if="brand.heritage_image" class="bs_heritage_media">
                    <img t-att-src="'/web/image/bs.car.brand/%s/heritage_image/1600x1067' % brand.id"
                         t-att-alt="brand.name" loading="lazy"/>
                </div>
                <div t-else="" class="bs_heritage_media bs_heritage_media_fallback"/>
                <div class="bs_heritage_scrim"/>
                <div class="container bs_heritage_copy">
                    <span class="car_kicker" t-esc="brand.tagline or brand.name"/>
                    <h2 class="bs_heritage_title" t-esc="brand.heritage_title or brand.name"/>
                    <div t-if="brand.heritage_html" class="bs_heritage_text" t-out="brand.heritage_html"/>
                    <a t-if="brand.heritage_cta_url" t-att-href="brand.heritage_cta_url"
                       class="btn btn-primary car_cta_btn">
                        <t t-esc="brand.heritage_cta_label or 'Explore the range'"/>
                        <i class="fa fa-arrow-right"/>
                    </a>
                </div>
            </div>
        </t>
        <div t-else="" class="bs_featured_empty alert alert-info text-center">
            Add a brand and tick <b>Featured Brand</b> (with a Heritage story) to populate this section.
        </div>
    </template>
    <template id="s_bs_heritage" name="Brand Heritage">
        <section class="s_bs_heritage" data-snippet="s_bs_heritage" data-name="Brand Heritage">
            <t t-call="bs_car_booking.heritage_content"/>
        </section>
    </template>


    <!-- ================================================================
         LATEST ARRIVALS — newest published models (by arrival date) with a
         "New" badge. Backend-driven via bs.car.model.arrival_date.
         ================================================================ -->
    <template id="latest_arrivals_content" name="Latest Arrivals (content)">
        <t t-set="arrivals" t-value="env['bs.car.model'].sudo()._get_latest_arrivals(limit=4)"/>
        <t t-if="arrivals">
            <div class="container bs_arrivals">
                <div class="section_header_center">
                    <span class="car_kicker car_kicker_dark">Just landed</span>
                    <h2 class="section_title">Latest arrivals</h2>
                </div>
                <div class="car_grid">
                    <div t-foreach="arrivals" t-as="car" class="bs_catalog_card" t-att-data-href="'/car/%s' % car.id">
                        <div class="bs_catalog_card_media">
                            <img t-if="car.image" loading="lazy" t-att-alt="car.name"
                                 t-att-src="'/web/image/bs.car.model/%s/image/800x534' % car.id"/>
                            <div t-else="" class="bs_catalog_card_placeholder">
                                <i class="fa fa-car"/>
                            </div>
                            <span t-if="car.is_new_arrival" class="bs_card_badge bs_card_badge_new">New</span>
                            <span t-if="car.body_type" class="bs_card_badge bs_card_badge_type" t-esc="car.body_type.title()"/>
                            <div class="bs_catalog_card_overlay"/>
                        </div>
                        <div class="bs_catalog_card_body">
                            <div class="bs_catalog_card_head">
                                <span class="bs_card_brand" t-esc="car.brand_id.name"/>
                            </div>
                            <h3 class="bs_card_title" t-esc="car.name"/>
                            <div class="bs_catalog_card_footer">
                                <div class="bs_card_price" t-if="car.base_price">
                                    <span class="bs_card_price_label">From</span>
                                    <strong t-esc="car.base_price"
                                            t-options="{'widget': 'monetary', 'display_currency': car.currency_id}"/>
                                </div>
                                <div class="bs_card_actions">
                                    <a t-if="car.is_available" t-att-href="'/car/%s/book' % car.id" class="bs_card_book_btn">
                                        Order Now
                                    </a>
                                    <a t-att-href="'/car/%s' % car.id" class="bs_card_cta">
                                        Discover <i class="fa fa-arrow-right"/>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </t>
        <div t-else="" class="bs_featured_empty alert alert-info text-center">
            Publish a car model (and set its Arrival Date) to populate this section.
        </div>
    </template>
    <template id="s_bs_arrivals" name="Latest Arrivals">
        <section class="s_bs_arrivals car_catalog_band" data-snippet="s_bs_arrivals" data-name="Latest Arrivals">
            <t t-call="bs_car_booking.latest_arrivals_content"/>
        </section>
    </template>

    <!-- ================================================================
         MODEL STAGE — single-model campaign stage.
         Data source: bs.car.website.section + bs.car.showcase.item(item_type='stage').
         ================================================================ -->
    <template id="model_stage_content" name="Model Stage (content)">
        <t t-set="section" t-value="model_stage_section or False"/>
        <t t-set="car" t-value="featured_car or (section and section._section_model()) or env['bs.car.model'].sudo()._get_website_featured()"/>
        <t t-if="car">
            <t t-set="stage_items" t-value="env['bs.car.showcase.item'].sudo()._get_website_items(car, 'stage', limit=3)"/>
            <div class="bs_model_stage" data-bs-showcase-stage="1">
                <div class="bs_model_stage_slides">
                    <article t-if="stage_items" t-foreach="stage_items" t-as="item"
                             t-attf-class="bs_model_stage_slide #{item_index == 0 and 'is-active' or ''}"
                             t-att-data-stage-index="item_index">
                        <img t-if="item.image"
                             t-att-src="'/web/image/bs.car.showcase.item/%s/image/1920x1080' % item.id"
                             t-att-alt="item.image_alt or item.name"/>
                        <div t-else="" class="bs_model_stage_fallback"/>
                    </article>
                    <article t-elif="section and section.image" class="bs_model_stage_slide is-active" data-stage-index="0">
                        <img t-att-src="'/web/image/bs.car.website.section/%s/image/1920x1080' % section.id"
                             t-att-alt="section.image_alt or section.title or car.name"/>
                    </article>
                    <article t-elif="car.image" class="bs_model_stage_slide is-active" data-stage-index="0">
                        <img t-att-src="'/web/image/bs.car.model/%s/image/1920x1080' % car.id"
                             t-att-alt="car.name"/>
                    </article>
                    <article t-else="" class="bs_model_stage_slide is-active" data-stage-index="0">
                        <div class="bs_model_stage_fallback"/>
                    </article>
                </div>
                <div class="bs_model_stage_label">
                    <span t-if="(section and section.eyebrow) or car.brand_id" class="bs_model_stage_kicker">
                        <t t-esc="(section and section.eyebrow) or car.brand_id.name"/>
                    </span>
                    <t t-if="section and section.title_image">
                        <strong class="visually-hidden" t-esc="section.title or car.name"/>
                        <img class="bs_model_stage_title_img"
                             t-att-src="'/web/image/bs.car.website.section/%s/title_image/1200x330' % section.id"
                             t-att-alt="section.title_image_alt or section.title or car.name"/>
                    </t>
                    <strong t-else="" class="bs_model_stage_wordmark">
                        <t t-esc="(section and section.title) or car.name"/>
                    </strong>
                    <p t-if="section and section.subtitle" class="bs_model_stage_subtitle" t-esc="section.subtitle"/>
                    <div t-if="section and section.body_html" class="bs_model_stage_body" t-out="section.body_html"/>
                    <t t-set="stage_primary_url" t-value="section and (section.primary_cta_url or ('/car/%s' % car.id))"/>
                    <t t-set="stage_secondary_url" t-value="section and (section.secondary_cta_url or '/test-drive')"/>
                    <div t-if="section and (section.primary_cta_label or section.secondary_cta_label or section.primary_cta_url or section.secondary_cta_url)"
                         class="bs_model_stage_actions">
                        <a t-if="section.primary_cta_label or section.primary_cta_url"
                           t-att-href="stage_primary_url"
                           class="btn btn-primary car_cta_btn">
                            <t t-esc="section.primary_cta_label or 'Explore'"/>
                            <i class="fa fa-arrow-right"/>
                        </a>
                        <a t-if="section.secondary_cta_label or section.secondary_cta_url"
                           t-att-href="stage_secondary_url"
                           class="btn btn-outline-light car_ghost_btn">
                            <t t-esc="section.secondary_cta_label or 'Learn more'"/>
                        </a>
                    </div>
                </div>
                <div t-if="len(stage_items) &gt; 1" class="bs_model_stage_dots">
                    <button t-foreach="stage_items" t-as="item" type="button"
                            t-attf-class="bs_stage_dot #{item_index == 0 and 'is-active' or ''}"
                            t-att-data-stage-dot="item_index"
                            t-att-aria-label="'Show %s' % item.name"/>
                </div>
            </div>
        </t>
        <div t-else="" class="bs_featured_empty alert alert-info text-center">
            Publish a car model to populate this section.
        </div>
    </template>
    <template id="s_bs_model_stage" name="Model Stage">
        <section class="s_bs_model_stage" data-snippet="s_bs_model_stage" data-name="Model Stage">
            <t t-set="model_stage_section" t-value="False"/>
            <t t-set="featured_car" t-value="False"/>
            <t t-call="bs_car_booking.model_stage_content"/>
        </section>
    </template>

    <!-- ================================================================
         COLOUR STUDIO — exterior + interior option imagery.
         Data source: bs.car.showcase.item(item_type='exterior'/'interior').
         ================================================================ -->
    <template id="color_studio_content" name="Colour Studio (content)">
        <t t-set="section" t-value="color_studio_section or False"/>
        <t t-set="car" t-value="featured_car or (section and section._section_model()) or env['bs.car.model'].sudo()._get_website_featured()"/>
        <t t-if="car">
            <!-- Configurator-option-driven Colour Studio (preferred): swatches +
                 imagery come from the priced options on bs.car.model.option, and
                 the interior set is filtered by the chosen exterior colour. Falls
                 back to the showcase-item studio below when no option imagery
                 has been set up for the model. -->
            <t t-set="ext_options" t-value="env['bs.car.model.option'].sudo()._get_color_options(car, 'bs_car_booking.attr_color')"/>
            <t t-set="int_options" t-value="env['bs.car.model.option'].sudo()._get_color_options(car, 'bs_car_booking.attr_interior')"/>
            <t t-if="ext_options.filtered('image') or int_options.filtered('image')">
                <div class="container bs_color_studio bs_color_studio_linked" data-color-studio-linked="1">
                    <div class="section_header_center">
                        <span class="car_kicker car_kicker_dark">
                            <t t-esc="(section and section.eyebrow) or 'Colour Options'"/>
                        </span>
                        <h2 t-if="section and section.title" class="section_title" t-esc="section.title"/>
                        <p t-if="section and section.subtitle" class="section_subtitle" t-esc="section.subtitle"/>
                    </div>
                    <!-- Exterior image: full-bleed body shot of the selected colour. -->
                    <div class="bs_cs_stage bs_cs_stage_ext" data-cs-stage="exterior">
                        <img t-foreach="ext_options" t-as="option"
                             t-attf-class="bs_color_image bs_color_image_exterior #{option_index == 0 and 'is-active' or ''}"
                             t-att-data-showcase-image="'ext-%s' % option.id"
                             t-att-src="'/web/image/bs.car.model.option/%s/image/1920x1080' % option.id"
                             t-att-alt="option.image_alt or option.display_name"/>
                        <!-- <span class="bs_cs_caption" data-cs-caption="1"/> -->
                    </div>
                    <!-- Centred selector strip: exterior squares flanking the
                         chosen exterior's interiors (boxed, expands on select). -->
                    <div class="bs_cs_bar">
                        <div t-if="ext_options" class="bs_cs_group bs_cs_exterior" data-showcase-group="exterior">
                            <button t-foreach="ext_options" t-as="option" type="button"
                                    t-attf-class="bs_color_btn #{option_index == 0 and 'is-active' or ''}"
                                    t-att-data-showcase-target="'ext-%s' % option.id"
                                    t-att-data-interior-ids="','.join(['int-%s' % i for i in (option.interior_option_ids.ids or int_options.ids)])"
                                    t-att-title="option.value_id.name">
                                <span class="bs_color_swatch"
                                      t-att-style="option._studio_swatch_style()"/>
                                <span class="bs_color_label" t-esc="option.value_id.name"/>
                            </button>
                        </div>
                        <div t-if="int_options" class="bs_cs_group bs_cs_interior" data-showcase-group="interior">
                            <button t-foreach="int_options" t-as="option" type="button"
                                    t-attf-class="bs_color_btn #{option_index == 0 and 'is-active' or ''}"
                                    t-att-data-showcase-target="'int-%s' % option.id"
                                    t-att-title="option.value_id.name">
                                <span class="bs_color_swatch"
                                      t-att-style="option._studio_swatch_style()"/>
                                <span class="bs_color_label" t-esc="option.value_id.name"/>
                            </button>
                        </div>
                    </div>
                    <!-- Interior image: full-bleed cabin shot, shown below the
                         selector for the chosen interior colour. -->
                    <div t-if="int_options" class="bs_cs_stage bs_cs_stage_int" data-cs-stage="interior">
                        <img t-foreach="int_options" t-as="option"
                             t-attf-class="bs_color_image #{option_index == 0 and 'is-active' or ''}"
                             t-att-data-showcase-image="'int-%s' % option.id"
                             t-att-src="'/web/image/bs.car.model.option/%s/image/1920x1080' % option.id"
                             t-att-alt="option.image_alt or option.display_name"/>
                        <!-- <span class="bs_cs_caption" data-cs-caption="1"/> -->
                    </div>
                </div>
            </t>
            <t t-else="">
            <t t-set="exteriors" t-value="env['bs.car.showcase.item'].sudo()._get_website_items(car, 'exterior')"/>
            <t t-set="interiors" t-value="env['bs.car.showcase.item'].sudo()._get_website_items(car, 'interior')"/>
            <t t-if="exteriors or interiors">
                <div class="container bs_color_studio bs_color_studio_linked" data-color-studio-linked="1">
                    <div class="section_header_center">
                        <span class="car_kicker car_kicker_dark">
                            <t t-esc="(section and section.eyebrow) or 'Colour Options'"/>
                        </span>
                        <h2 t-if="section and section.title" class="section_title" t-esc="section.title"/>
                        <p t-if="section and section.subtitle" class="section_subtitle" t-esc="section.subtitle"/>
                    </div>
                    <!-- Shared stage: one large image; exterior bodies and
                         interior cabins swap into the same frame. -->
                    <div class="bs_cs_stage" data-cs-stage="1">
                        <img t-foreach="exteriors" t-as="item"
                             t-attf-class="bs_color_image bs_color_image_exterior #{item_index == 0 and 'is-active' or ''}"
                             t-att-data-showcase-image="'ext-%s' % item.id"
                             t-att-src="'/web/image/bs.car.showcase.item/%s/image/1600x900' % item.id"
                             t-att-alt="item.image_alt or item.name"/>
                        <img t-foreach="interiors" t-as="item"
                             class="bs_color_image"
                             t-att-data-showcase-image="'int-%s' % item.id"
                             t-att-src="'/web/image/bs.car.showcase.item/%s/image/1600x900' % item.id"
                             t-att-alt="item.image_alt or item.name"/>
                        <!-- <span class="bs_cs_caption" data-cs-caption="1"/> -->
                    </div>
                    <!-- Single centred selector strip: exterior squares flanking
                         the interiors (boxed). -->
                    <div class="bs_cs_bar">
                        <div t-if="exteriors" class="bs_cs_group bs_cs_exterior" data-showcase-group="exterior">
                            <button t-foreach="exteriors" t-as="item" type="button"
                                    t-attf-class="bs_color_btn #{item_index == 0 and 'is-active' or ''}"
                                    t-att-data-showcase-target="'ext-%s' % item.id"
                                    t-att-title="item.name">
                                <span class="bs_color_swatch"
                                      t-att-style="item._studio_swatch_style()"/>
                                <span class="bs_color_label" t-esc="item.name"/>
                            </button>
                        </div>
                        <div t-if="interiors" class="bs_cs_group bs_cs_interior" data-showcase-group="interior">
                            <button t-foreach="interiors" t-as="item" type="button"
                                    t-attf-class="bs_color_btn #{item_index == 0 and 'is-active' or ''}"
                                    t-att-data-showcase-target="'int-%s' % item.id"
                                    t-att-title="item.name">
                                <span class="bs_color_swatch"
                                      t-att-style="item._studio_swatch_style()"/>
                                <span class="bs_color_label" t-esc="item.name"/>
                            </button>
                        </div>
                    </div>
                </div>
            </t>
            </t>
        </t>
    </template>
    <template id="s_bs_color_studio" name="Colour Studio">
        <section class="s_bs_color_studio car_catalog_band" data-snippet="s_bs_color_studio" data-name="Colour Studio">
            <t t-set="featured_car" t-value="False"/>
            <t t-set="color_studio_section" t-value="False"/>
            <t t-call="bs_car_booking.color_studio_content"/>
        </section>
    </template>

    <!-- ================================================================
         HIGHLIGHTS — editorial E-HS9 feature cards.
         Data source: bs.car.showcase.item(item_type='highlight').
         ================================================================ -->
    <template id="highlights_content" name="Highlights (content)">
        <t t-set="section" t-value="highlights_section or False"/>
        <t t-set="car" t-value="featured_car or env['bs.car.model'].sudo()._get_website_featured()"/>
        <t t-if="car">
            <t t-set="items" t-value="env['bs.car.showcase.item'].sudo()._get_website_items(car, 'highlight')"/>
            <t t-if="items">
                <div class="container bs_highlights">
                    <div class="section_header_center">
                        <span class="car_kicker car_kicker_dark">
                            <t t-esc="(section and section.eyebrow) or '%s Highlights' % car.name"/>
                        </span>
                        <h2 class="section_title">
                            <t t-esc="(section and section.title) or 'Signature details, engineered for arrival'"/>
                        </h2>
                        <p t-if="section and section.subtitle" class="section_lead" t-esc="section.subtitle"/>
                    </div>
                    <div class="bs_highlight_grid">
                        <article t-foreach="items" t-as="item" class="bs_highlight_card"
                                 role="button" tabindex="0"
                                 t-att-data-hl-img="'/web/image/bs.car.showcase.item/%s/image/1600x1067' % item.id"
                                 t-att-data-hl-kicker="car.brand_id.name + ' ' + car.name"
                                 t-att-data-hl-title="item.name"
                                 t-att-data-hl-desc="item.description or item.subtitle or ''"
                                 t-att-data-hl-gallery="item._highlight_gallery_json()">
                            <span class="bs_highlight_index">
                                <t t-esc="'%02d' % (item_index + 1)"/>
                            </span>
                            <img t-if="item.image"
                                 t-att-src="'/web/image/bs.car.showcase.item/%s/image/900x600' % item.id"
                                 t-att-alt="item.image_alt or item.name" loading="lazy"/>
                            <div t-else="" class="bs_highlight_fallback"/>
                            <span class="bs_highlight_zoom"><i class="fa fa-expand"/></span>
                            <span t-if="item.highlight_image_ids" class="bs_highlight_gallery_count">
                                <i class="fa fa-picture-o"/>
                                <t t-esc="len(item.highlight_image_ids) + (1 if item.image else 0)"/>
                            </span>
                            <div class="bs_highlight_body">
                                <span t-if="item.subtitle" class="bs_highlight_kicker" t-esc="item.subtitle"/>
                                <h3 t-esc="item.name"/>
                                <div t-if="item.description" class="bs_highlight_desc" t-out="item.description"/>
                                <span class="bs_highlight_more">
                                    View detail <i class="fa fa-arrow-right"/>
                                </span>
                            </div>
                        </article>
                    </div>
                </div>
            </t>
        </t>
    </template>
    <template id="s_bs_highlights" name="Highlights">
        <section class="s_bs_highlights car_catalog_band" data-snippet="s_bs_highlights" data-name="Highlights">
            <t t-set="featured_car" t-value="False"/>
            <t t-set="highlights_section" t-value="False"/>
            <t t-call="bs_car_booking.highlights_content"/>
        </section>
    </template>

    <!-- ================================================================
         CABIN STORY — cinematic image/text story band.
         Data source: first bs.car.showcase.item(item_type='cabin').
         ================================================================ -->
    <template id="cabin_story_content" name="Cabin Story (content)">
        <t t-set="section" t-value="cabin_story_section or False"/>
        <t t-set="car" t-value="featured_car or (section and section._section_model()) or env['bs.car.model'].sudo()._get_website_featured()"/>
        <!-- Image fallback: the model's 'cabin' showcase item, so the section
             still renders before a dealer uploads its own cabin media. -->
        <t t-set="cabin_item" t-value="car and env['bs.car.showcase.item'].sudo()._get_website_items(car, 'cabin', limit=1)[:1]"/>
        <t t-set="has_section_media" t-value="section and (section.video_src or section.video_embed_url or section.image)"/>
        <t t-if="(section and (has_section_media or section.title)) or cabin_item">
            <div class="bs_cabin_story">
                <div class="bs_cabin_media">
                    <t t-if="section and section.video_src">
                        <video t-attf-class="bs_cabin_img bs_hero_video #{section.mobile_video_src and 'bs_hero_video_desktop' or ''}"
                               autoplay="autoplay" muted="muted" loop="loop" playsinline="playsinline"
                               preload="metadata" disablepictureinpicture="disablepictureinpicture"
                               t-att-poster="section.image and '/web/image/bs.car.website.section/%s/image/1920x1080' % section.id or None">
                            <source t-att-src="section.video_src" type="video/mp4"/>
                        </video>
                        <video t-if="section.mobile_video_src"
                               class="bs_cabin_img bs_hero_video bs_hero_video_mobile"
                               autoplay="autoplay" muted="muted" loop="loop" playsinline="playsinline"
                               preload="metadata" disablepictureinpicture="disablepictureinpicture"
                               t-att-poster="section.image and '/web/image/bs.car.website.section/%s/image/1200x1600' % section.id or None">
                            <source t-att-src="section.mobile_video_src" type="video/mp4"/>
                        </video>
                    </t>
                    <t t-elif="section and section.video_embed_url">
                        <div class="bs_cabin_embed" aria-hidden="true">
                            <iframe t-att-src="section.video_embed_url" title="Cabin video"
                                    allow="autoplay; fullscreen"
                                    referrerpolicy="strict-origin-when-cross-origin" loading="lazy"/>
                        </div>
                        <img t-if="section.image" class="bs_cabin_img bs_hero_poster"
                             t-att-src="'/web/image/bs.car.website.section/%s/image/1920x1080' % section.id"
                             t-att-alt="section.image_alt or section.title"/>
                    </t>
                    <img t-elif="section and section.image" class="bs_cabin_img"
                         t-att-src="'/web/image/bs.car.website.section/%s/image/1920x1080' % section.id"
                         t-att-alt="section.image_alt or section.title" loading="lazy"/>
                    <img t-elif="cabin_item and cabin_item.image" class="bs_cabin_img"
                         t-att-src="'/web/image/bs.car.showcase.item/%s/image/1920x1080' % cabin_item.id"
                         t-att-alt="cabin_item.image_alt or cabin_item.name" loading="lazy"/>
                </div>
                <div class="bs_cabin_scrim"/>
                <div class="container bs_cabin_copy">
                    <span class="car_kicker"><t t-esc="(section and section.eyebrow) or (cabin_item and cabin_item.subtitle) or 'Crafted Sanctuary'"/></span>
                    <h2 class="bs_heritage_title" t-esc="(section and section.title) or (cabin_item and cabin_item.name) or 'A cabin composed in silence'"/>
                    <div t-if="section and section.subtitle" class="bs_heritage_text"><t t-esc="section.subtitle"/></div>
                    <div t-elif="cabin_item and cabin_item.description" class="bs_heritage_text" t-out="cabin_item.description"/>
                    <a t-if="section and section.primary_cta_url" t-att-href="section.primary_cta_url" class="btn btn-primary car_cta_btn">
                        <t t-esc="section.primary_cta_label or 'Experience this model'"/> <i class="fa fa-arrow-right"/>
                    </a>
                    <a t-elif="cabin_item and cabin_item.cta_url" t-att-href="cabin_item.cta_url" class="btn btn-primary car_cta_btn">
                        <t t-esc="cabin_item.cta_label or 'Experience this model'"/> <i class="fa fa-arrow-right"/>
                    </a>
                </div>
            </div>
        </t>
    </template>
    <template id="s_bs_cabin_story" name="Cabin Story">
        <section class="s_bs_cabin_story" data-snippet="s_bs_cabin_story" data-name="Cabin Story">
            <t t-set="featured_car" t-value="False"/>
            <t t-set="cabin_story_section" t-value="False"/>
            <t t-call="bs_car_booking.cabin_story_content"/>
        </section>
    </template>

    <!-- ================================================================
         LATEST STORIES — brand/editorial story cards.
         Data source: bs.car.story.
         ================================================================ -->
    <template id="stories_content" name="News (content)">
        <t t-set="story_model" t-value="featured_car if featured_car else False"/>
        <t t-set="stories" t-value="env['bs.car.story'].sudo()._get_website_stories(model_id=story_model.id if story_model else False, limit=3)"/>
        <t t-if="stories">
            <div class="container bs_stories">
                <div class="section_header_center">
                    <span class="car_kicker car_kicker_dark">News</span>
                    <h2 class="section_title">The journey behind the red flag</h2>
                </div>
                <div class="bs_story_grid">
                    <a t-foreach="stories" t-as="story" t-att-href="story._get_website_url()" class="bs_story_card">
                        <img t-if="story.image"
                             t-att-src="'/web/image/bs.car.story/%s/image/900x600' % story.id"
                             t-att-alt="story.name" loading="lazy"/>
                        <div class="bs_story_body">
                            <span t-if="story.subtitle" class="bs_story_kicker" t-esc="story.subtitle"/>
                            <h3 t-esc="story.name"/>
                            <p t-if="story.excerpt" t-esc="story.excerpt"/>
                            <span class="bs_story_link">
                                <t t-esc="story.cta_label or 'Read more'"/> <i class="fa fa-arrow-right"/>
                            </span>
                        </div>
                    </a>
                </div>
            </div>
        </t>
    </template>
    <template id="s_bs_stories" name="Latest News">
        <section class="s_bs_stories car_catalog_band" data-snippet="s_bs_stories" data-name="Latest News">
            <t t-call="bs_car_booking.stories_content"/>
        </section>
    </template>

    <!-- Register the snippets in the website builder blocks panel. -->
    <template id="bs_car_booking_snippets" inherit_id="website.snippets" name="BS Car Booking Snippets">
        <xpath expr="//t[@t-snippet='website.s_cover']" position="after">
            <t t-snippet="bs_car_booking.s_bs_brand_cover" string="Brand Cover" group="intro"/>
            <t t-snippet="bs_car_booking.s_bs_featured_model" string="Featured Car" group="intro"/>
            <t t-snippet="bs_car_booking.s_bs_model_stage" string="Model Stage" group="intro"/>
            <t t-snippet="bs_car_booking.s_bs_color_studio" string="Colour Studio" group="intro"/>
            <t t-snippet="bs_car_booking.s_bs_heritage" string="Brand Heritage" group="intro"/>
            <t t-snippet="bs_car_booking.s_bs_model_specs" string="Model Specs" group="intro"/>
            <t t-snippet="bs_car_booking.s_bs_highlights" string="Highlights" group="intro"/>
            <t t-snippet="bs_car_booking.s_bs_cabin_story" string="Cabin Story" group="intro"/>
            <t t-snippet="bs_car_booking.s_bs_arrivals" string="Latest Arrivals" group="intro"/>
            <t t-snippet="bs_car_booking.s_bs_offers" string="Offers &amp; Promotions" group="intro"/>
            <t t-snippet="bs_car_booking.s_bs_stories" string="Latest News" group="intro"/>
            <t t-snippet="bs_car_booking.s_bs_dealers" string="Dealer Locator" group="intro"/>
        </xpath>
    </template>
</odoo>
